{
  "name": "uj-payments-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/index.js",
    "test": "vitest run"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "uqr": "^0.1.3"
  }
}
